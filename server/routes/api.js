const path = require('path');
const express = require('express');

const router = express.Router();

// GET "api/song"   > send over mp3/4
router.get('/song', (req, res) => {});

// POST "api/song" > sending answer 'string'
router.post('/song', (req, res) => {});


